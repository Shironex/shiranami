[package]
name = "shiranami-recommendation"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
shiranami-core.workspace = true
shiranami-db.workspace = true
shiranami-downloader.workspace = true

[dev-dependencies]
# Tests only. `serde` reads the recorded inputs out of the golden-vector
# fixture; `serde_json` both parses it and asserts that a generated mix `id`
# still equals the serde name of its `SmartMixKind` — the renderer keys on that
# string, and the port put the two in separate places for the first time.
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
