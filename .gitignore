node_modules/
dist/
build/
# Cargo build output (root workspace + any nested target dir). Cargo.lock IS
# tracked — it doubles as the lockfile-drift guard for the Rust tier.
target/
# Tauri regenerates these permission schemas from the plugin set on every build.
apps/desktop-tauri/src-tauri/gen/
bin/
release/
*.log
.turbo/
.env
.env.local
.DS_Store
Thumbs.db
# Editor-generated, machine-specific config (absolute paths, per-extension).
.vscode/
*.tsbuildinfo
.claude
.mcp.json
coverage
.omniscribe
.playwright-mcp
docs/*
# docs/v2 is the tracked home of the Rust/Tauri rewrite plan, not agent scratch.
!docs/v2/
# Versioned migration folders under packages/database/drizzle/ ARE tracked
# (they are the canonical migration ledger). Only ignore local dev databases.
packages/database/data/
packages/database/drizzle/**/*.db
packages/database/drizzle/**/*.db-*
playwright-report/
test-results/
apps/desktop/e2e/.tmp/
/design/
# Storybook static build output
apps/web/storybook-static/
# design-sync (claude.ai/design) — transient/scratch state
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.design-sync/old-project-backup/
.design-sync/config.json.bak
.ds-sync/
ds-bundle/

# generated DS bundle barrel (design-sync) — outputs, not the generator
apps/web/ds-entry.generated.ts
apps/web/index.d.ts

# per-release Discord announcement scratch file (overwritten each release)
docs/release-announcement.md
