/** Bottom player bar height in px. Used by App.tsx content padding,
 *  the visualizer strip offset, and PlayerBar's own height + slide-in motion. */
export const PLAYER_BAR_HEIGHT = 88;
/** Height of the visualizer strip that sits directly above the player bar. */
export const VISUALIZER_HEIGHT = 48;
/** Content bottom padding when both the player bar AND the visualizer strip show. */
export const PLAYER_BAR_PLUS_VIZ = PLAYER_BAR_HEIGHT + VISUALIZER_HEIGHT; // 136
