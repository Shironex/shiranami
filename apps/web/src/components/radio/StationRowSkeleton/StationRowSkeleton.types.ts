/**
 * StationRowSkeleton renders a fixed, input-free placeholder row, so its props
 * and view surfaces are intentionally empty — they exist to keep the
 * per-component contract shape consistent across the feature.
 */
export interface IStationRowSkeletonProps {}

export interface IStationRowSkeletonView {}
