/**
 * SplashCup is fixed product art with no inputs, so its props and view surfaces
 * are intentionally empty — they exist to keep the per-component contract shape
 * consistent across the feature.
 */
export interface ISplashCupProps {}

export interface ISplashCupView {}
