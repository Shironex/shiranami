/**
 * SplashGlass renders fixed, input-free glass texture, so its props and view
 * surfaces are intentionally empty — they exist to keep the per-component
 * contract shape consistent across the feature.
 */
export interface ISplashGlassProps {}

export interface ISplashGlassView {}
