/**
 * DownloadsSectionSkeleton renders a fixed, input-free placeholder for the
 * downloads settings card, so its props and view surfaces are intentionally
 * empty — they exist to keep the per-component contract shape consistent.
 */
export interface IDownloadsSectionSkeletonProps {}

export interface IDownloadsSectionSkeletonView {}
