{
  "name": "@shiranami/desktop-tauri",
  "version": "2.0.0-alpha.0",
  "private": true,
  "description": "Shiranami v2 desktop shell (Tauri) — the Rust backend lives in src-tauri and crates/*",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "e2e:build": "pnpm -w run build:packages && pnpm -w --filter @shiranami/web build && cargo build -p shiranami-desktop --features e2e --bin shiranami-desktop",
    "test:e2e": "wdio run e2e/wdio.conf.ts",
    "test:e2e:build": "pnpm run e2e:build && pnpm run test:e2e"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0",
    "@types/mocha": "^10.0.10",
    "@wdio/cli": "^9.27.1",
    "@wdio/globals": "^9.27.1",
    "@wdio/local-runner": "^9.27.1",
    "@wdio/mocha-framework": "^9.27.1",
    "@wdio/spec-reporter": "^9.27.1",
    "@wdio/tauri-service": "^1.2.0",
    "webdriverio": "^9.27.1"
  }
}
